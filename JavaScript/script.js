const printHello = () => {
    alert ('Hola')
}
const addbutton = document.getElementById('addbutton')
const Contacts = {}
const contactsKey = 'Contacts'
addbutton.addEventListener('click', addContacts)

Contacts.push (name)
localStorage.setItem(contactsKey, Matias.stringify( Contacts))

const addContacts = (e) => {e.preventDefault()
console.log(e);
}
const getContacts = () => {} 
const updateContact = (id, name) => {}
const deleteContact = (id) => {}

getContacts()

